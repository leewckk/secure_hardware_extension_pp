


#ifndef KEY_SLOTS_H
#define KEY_SLOTS_H

/**
 * Enum to be inherited by user.
 * Every silicon manufacturer may implement its own key slot identification.
 */
class KeySlotsBase {
public:
    enum Slot {
        // Placeholder for derived classes to define their own key slots
    };
};

#endif // KEY_SLOTS_H
